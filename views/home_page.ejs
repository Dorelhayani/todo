<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home Page</title>
    <style>
        * {
            font-family: Helvetica, Arial, sans-serif;
            box-sizing: border-box;
        }

        body {
            background-color: #f2f2f2;
            margin: 0;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h2 {
            color: #333;
            margin-bottom: 30px;
        }

        .menu {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            max-width: 800px;
        }

        .menu button {
            padding: 15px 25px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            color: #fff;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: background-color 0.2s ease;
        }

        .btn-category-add {
            background-color: #007bff; /* blue */
        }

        .btn-category-list {
            background-color: #6c757d; /* gray */
        }

        .btn-task-add {
            background-color: #28a745; /* green */
        }

        .btn-task-list {
            background-color: #17a2b8; /* teal */
        }

        .btn-user-list {
            background-color: #ffc107; /* yellow */
            color: #000;
        }

        .btn-user-add {
            background-color: #dc3545; /* red */
        }

        .menu button:hover {
            opacity: 0.85;
        }
    </style>
</head>
<body>

<h2>Welcome, <%= name %>!</h2>

<div class="menu">
    <button class="btn-category-add" onclick="location.href='/category/add'">Add Category</button>
    <button class="btn-category-list" onclick="location.href='/category/list'">Category List</button>
    <button class="btn-task-add" onclick="location.href='/task/add'">Add Task</button>
    <button class="btn-task-list" onclick="location.href='/task/list'">Task List</button>
    <button class="btn-user-list" onclick="location.href='/users/list'">Users List</button>
    <button class="btn-user-add" onclick="location.href='/users/add'">Add User</button>
</div>

</body>
</html>
